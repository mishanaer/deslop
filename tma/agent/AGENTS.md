## TMA

- Перед любой работой с интерфейсом прочитай `.deslop/tma/COMPONENTS.md` и выбери существующий компонент по назначению.
- Собирай интерфейс только из локальных компонентов `src/components/tma`. Не импортируй продуктовые компоненты напрямую из `@deslop/tma`, не создавай их аналоги и не подключай другие UI-библиотеки.
- Если нужно понять устройство компонента, читай его исходники в `.deslop/tma/source`. Не импортируй код из этой справочной папки.
- Для кнопок, полей, переключателей, строк, панелей, меню, модалок, навигации, обратной связи и загрузки всегда используй соответствующий компонент TMA, а не сырой HTML-контрол.
- Используй только стили, токены, шрифты и иконки Deslop. Не добавляй хардкод цветов, размеров, отступов, радиусов, теней, локальные SVG, inline styles или Tailwind arbitrary values.
- Если нужного компонента нет, остановись и сообщи, какой компонент нужно добавить в TMA. Не создавай замену внутри продукта без прямого запроса пользователя.
- Отступать от этих правил можно только по прямому запросу пользователя. Исключение нужно явно назвать в результате работы.
- Перед завершением изменений интерфейса запускай `npm run check:tma` и исправляй нарушения, а не отключай проверку.
